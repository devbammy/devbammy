import styles from './loader.module.scss';

const Loader = () => {
  return (
    <section className={styles.loader}>
      <h1 className={styles.brand}>bamidele.dev</h1>
    </section>
  );
};

export default Loader;
