@use '../../../styles/_variables' as *;

// Works Section Container
.marqueeSection {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  padding: 4rem 0;

  // Handles widescreen layout break breakouts cleanly
  position: relative;
  left: 50%;
  transform: translateX(-50%);
  width: 100vw;
  overflow: hidden;

  @media (min-width: 1800px) {
    width: 100%;
    left: 0;
    transform: none;
  }

  /* ==========================================================================
     MOBILE RESPONSIVENESS CAPTURE
     ========================================================================== */
  @media (max-width: 640px) {
    // 1. Resets the viewport break back into the normal document flow
    width: 100%;
    left: 0;
    transform: none;
    padding: 2.5rem 0;

    // 2. Adds the clean layout padding margins you want on the left and right!
    padding-left: 1.25rem;
    padding-right: 1.25rem;
  }
}

.marqueeRow {
  display: flex;
  width: 100%;
  overflow: hidden;
  position: relative;

  @media (max-width: 640px) {
    // Forces the row boundaries to inherit the parent's padding cuts cleanly
    border-radius: var(--radius);
  }

  // High-fidelity fading shadow edges
  &::before,
  &::after {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    width: 80px;
    z-index: 5;
    pointer-events: none;

    @media (max-width: 640px) {
      width: 40px; // Sized down slightly on phones so it doesn't wash out small cards
    }
  }
  &::before {
    left: 0;
    background: linear-gradient(
      to right,
      var(--container) 0%,
      transparent 100%
    );
  }
  &::after {
    right: right;
    background: linear-gradient(to left, var(--container) 0%, transparent 100%);
  }
}

.marquee {
  display: flex;
  align-items: center;
  gap: 16px; // Tighter inline separation gap for compact mobile layouts
  width: max-content;
  will-change: transform;
  animation: scrollX 40s linear infinite;

  @media (min-width: 641px) {
    gap: 24px;
  }

  &.reverse {
    animation-direction: reverse;
  }
}

// Adjusting standard card frame sizes down slightly on narrow mobile viewports
.mCard {
  position: relative;
  width: 220px;
  height: 140px;
  border-radius: var(--radius);
  border: 1px solid var(--grayAlt);
  background: var(--card);
  flex-shrink: 0;
  display: block;
  overflow: hidden !important;

  @media (min-width: 641px) {
    width: 280px;
    height: 180px;
  }

  // ... keep your existing hover rules, images and viewTag styles here ...
}
